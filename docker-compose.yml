services:
  nuxt_app:
    build: .
    container_name: laseda_fase2_frontal
    ports:
      - "3001:3000"
    volumes:
      - .:/app
    environment:
      - HOST=0.0.0.0
      - NODE_ENV=development